guessLetter = []
    incorrectLetter= []
    live = 6
    guess =['_']*len(words)

    while live > 0 and '_' in guess:
        # Display current progress
        print(" ".join(guess))
        print("Incorrect letters: ", ", ".join(incorrectLetter))
        hangman(6 - live)  # Display the hangman
        print(f"Live left: {live}")
        guesses = input("Guess a letter: ").lower()
                # Validate the guess
        if len(guesses) != 1 or not guesses.isalpha():
            print("Please enter a single letter.")
            continue

        if guesses in guessLetter:
            print("You've already guessed that letter.")
            continue

            # Check if the guessed letter is in the word
            guessLetter.append(guesses)

        if guesses in words:
            for i in range(len(words)):
                if words == guesses:
                    guess = guesses
                print("Correct!")
        else:
            live -= 1
            incorrectLetter.append(guesses)
            print("Incorrect!")

        if '_' not in guess:
            print(f"Congratulations, {name}! You've guessed the word:", words)
        else:
            print("Game over, you lose! The word was:", words)
            hangman(6)  # Display the last hanged man when losing

        # Ask if the player wants to play again
        play_again = input("Do you want to play again? (yes/no): ").lower()
        if play_again != "yes":
            print(f"Thank you for playing, {name}! Have a nice day!")
            return